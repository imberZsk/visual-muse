# 样式规范

- 延续现有 Ant Design 主题、间距、字号和状态色；同时检查明暗主题。
- 优先使用主题 token、现有 class 和组件属性，避免重复内联样式与无边界覆盖 antd 内部结构。
- 长标题、路径和标签支持省略、换行或 Tooltip，不得挤出主要操作。
- 使用稳定尺寸、`min-width: 0` 和响应式约束，loading 与动态内容不得造成布局跳动。
- 动画短促且不改变布局尺寸；不引入营销式装饰、过大圆角或无业务意义渐变。
