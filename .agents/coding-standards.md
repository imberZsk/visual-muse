# 代码规范

- 所有交流、注释和文档使用中文。
- 先通过代码、测试或复现定位根因，再做最小改动；不顺手重构或格式化无关代码。
- 只有存在实际复用时才抽离函数或组件。
- 状态、存储键、阈值和操作类型使用具名常量，禁止魔法字符串和数字。
- 访问对象、集合或接口返回值前处理空值；异步流程覆盖失败、取消和清理路径。
- 函数和关键参数写用途注释；业务变量说明存储内容；复杂逻辑与 workaround 注释 WHY。
- 阻塞型扫描、文件和进程操作使用异步 API，并提供统一 loading 状态。
- 优先复用现有依赖，新增依赖前评估打包体积、许可证和跨平台支持。
