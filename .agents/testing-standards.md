# 测试规范

- 测试只覆盖本次新增或修改行为；修复 Bug 时先增加可复现用例。
- UI 使用 Vitest、jsdom 与 Testing Library，按用户行为断言，不绑定无关内部实现。
- Electron 主进程能力使用依赖注入或 mock，不访问用户真实文件和配置。
- 临时文件在独立目录创建并清理；mock、计时器和全局对象在用例后恢复。
- 异步测试使用明确等待条件，不使用任意 sleep。
- 平台、路径和打包逻辑通过注入覆盖 macOS 与 Windows 分支。
