# 踩坑记录

- 开发环境依赖 `VITE_DEV_SERVER_URL`，生产环境从 `dist/` 加载；不能混用判断条件。
- 主进程/preload 由 `tsconfig.node.json` 构建到 `dist-electron/`，渲染层由 Vite 构建。
- Electron 生产环境使用本地资源，新增资源路径必须兼容打包后的文件位置。
- 文章素材与发布配置属于用户数据，解析单项失败应局部降级，不能破坏整个工作区。
- 编辑与发布准备流程可能耗时，异步期间防止重复操作并保留明确进度。
- 修改布局前检查长标题、长平台名称、窄窗口和明暗主题。
