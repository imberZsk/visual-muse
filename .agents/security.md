# 安全约束

- 不读取、打印或提交 token、密码、私钥、cookie、真实用户数据和带凭据 URL。
- 项目 `.npmrc` 只允许公开无凭据 registry；私有源和 token 放用户级配置。
- 保持 Electron 隔离配置与生产 CSP，不为局部问题整体放宽策略。
- IPC 输入视为不可信，校验类型、协议、路径归属和允许操作。
- 删除、覆盖、打开外部 URL 或执行命令前验证目标；动态值使用目标 shell 对应的安全引用。
- 构建产物不得包含用户目录、缓存、日志或本地配置。
