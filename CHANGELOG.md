# Changelog

本项目所有重要变更记录于此，遵循语义化版本规则。

## 0.5.0 - 2026-07-29

### 新增

- 接入小红书、掘金和微信公众号官方创作页面，支持隔离并持久化各平台登录会话，将 Markdown 标题和正文可靠填入真实编辑器，并在发布前执行平台级内容预检。
- 新增三平台分发计划，可从同一篇 Markdown 自动生成小红书可读纯文本、掘金 Markdown 和公众号富文本变体，并支持自动摘要、目标选择、一键批量准备、状态矩阵与异常单项重试。
- 三平台正文会自动移除与平台标题栏重复的开头一级标题，公众号分发同时填入自动生成或手动设置的摘要。
- 新增工作台、图文编辑、主题与 Skill 模板、AI 助手、自动任务、灵感热榜、账号模型、素材库、发布数据和使用指南等内容创作模块。
- 新增文档版本管理、多平台持久账号、加密凭据存储、草稿同步、Codex MCP 配置与工作区原子持久化能力。
- B 站草稿同步支持识别专栏 iframe 编辑器，自动填入标题和正文并触发保存为草稿。
- CSDN 草稿同步支持激活隐藏标题输入框、填入 Markdown 正文并保存到平台草稿箱。
- 百家号草稿同步支持跨 frame 填入标题和正文，并触发平台“存草稿”。
- 图文编辑新增完整手机预览壳，支持笔记页与双列封面流模式，并适配窄窗口稳定展示。

### 变更

- 移除无对应中文内容社区业务场景的 Medium 平台入口，并用真实草稿准备状态替代主界面的模拟发布记录。
- 重构右侧发布工作区为批量分发与平台设置两个任务视图，固定高频主操作、折叠公众号可选接口配置，并修复区块被压缩裁切导致无法滚动的问题。
- 拆分图文编辑器和功能页标题组件，收敛大型工作台文件的职责边界。

### 修复

- 兼容小红书长文发布的二段式入口，并允许无 frontmatter 的 Markdown 一级标题作为文章标题，避免真实发布时误报标题缺失或显示“未命名文章”。
- 修复掘金适配器只修改 CodeMirror 隐藏输入框、导致真实草稿正文保存为 0 字的问题，改为更新并回读编辑器文档模型；只有平台生成正式草稿 URL 且显示保存成功后才标记为“已保存”。
- 修复平台入口重定向到登录页时 Electron 抛出 `ERR_ABORTED`、导致正常的“需登录”状态被误报为任务失败的问题。
- 修复公众号标题和正文编辑器同为 ProseMirror 时正文误写进标题的问题，限定正文必须写入 `#ueditor_0`，并在成功前同时回读标题与正文内容。
- 修复平台编辑窗口仍打开时，关闭主工作台后无法通过 macOS Dock 重新打开的问题，改为独立跟踪并恢复主窗口。

## 0.4.1 - 2026-07-21

### 修复

- 兼容 `electron-updater` 的 CommonJS 导出结构，并在更新模块加载失败时安全降级，避免打包应用因更新器解析异常而无法启动。
- 修复当前版本仍错误展示更新入口的问题；Release 安装说明补充 `sudo`，公开资产严格限制为 macOS DMG、Windows 安装版和便携版三个用户安装包。
- macOS 安装包使用无空格的稳定文件名，避免 GitHub 上传时改名以及 Release 安装说明截断。
- 公开前清理 Git 历史中的个人提交邮箱，避免仓库公开后暴露非公开联系方式。

## 0.4.0 - 2026-07-15

### 新增

- 自动检查 GitHub Release，并支持先异步下载、下载完成后再安装重启。

## 0.3.0 - 2026-07-15

### 变更

- 统一升级至 React 19、Ant Design 6、Electron 43、Vite 8、TypeScript 6 与 pnpm 11.13，并完成 Electron/渲染进程构建兼容迁移。
- 统一 macOS arm64 与 Windows x64 双平台构建、测试和打包口径，并补齐开源协议、安全策略与行为准则。

## 0.2.0 - 2026-07-14

### 新增

- 接入 ESLint、Prettier、Husky、Commitlint 与 lint-staged，并在 CI 中执行完整代码检查。

## 0.1.1 - 2026-07-14

### 优化

- 统一发布工作台为简约黑白半透明主题，优化桌面与窄屏三栏布局、平台导航、编辑预览和发布配置的信息层级；补充持久化初始化、自动保存和模拟发布状态反馈，并修复启动时默认配置可能覆盖既有本地配置的问题。
- 平台配置按目标动态展示：微信公众号保留凭据与 Server 配置，掘金等内容平台改为分类、标签、摘要以及复制标题/正文、打开白名单创作入口。
- 提交公开的 npmmirror registry 配置，并为 Electron 打包命令配置 Electron 与 electron-builder 二进制镜像，减少依赖安装和打包下载耗时；私有凭据继续仅允许放在用户级 `.npmrc`。
